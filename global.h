#pragma once

#include <iostream>
#include <sstream>

#include <SFML/Graphics.hpp>

// font files
const std::string FONT_FILE = "Fonts/Dosis-Light.ttf";

// colors
const sf::Color BACKGROUND_COLOR = sf::Color(160, 100, 140);

const sf::Color TILE_COLOR = sf::Color(240, 240, 200);
const sf::Color TILE_OUTLINE = sf::Color(240, 40, 40);

const sf::Color BUTTON_COLOR = sf::Color(240, 240, 240);
const sf::Color BUTTON_OUTLINE = sf::Color(40, 40, 240);

const sf::Color MATCH_COLOR = sf::Color(120, 240, 120);